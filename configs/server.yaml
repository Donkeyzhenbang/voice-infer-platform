# Voice Inference Platform — 服务配置

server:
  host: "0.0.0.0"
  port: 8000
  log_level: info

websocket:
  max_message_size: 16777216    # 16MB
  ping_interval: 30
  ping_timeout: 10

session:
  max_history: 30               # 对话历史最大轮数
  idle_timeout: 300             # 空闲超时（秒）
